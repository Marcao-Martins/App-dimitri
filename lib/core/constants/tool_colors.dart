import 'package:flutter/material.dart';
import 'app_colors.dart';

/// Paleta de cores por ferramenta — use `ToolColors.<tool>` quando montar um ToolBox
class ToolColors {
  static const Color fluidotherapy = AppColors.primaryTeal;
  static const Color unespPain = AppColors.categoryPurple;
  static const Color autonomyO2 = AppColors.categoryBlue;
  static const Color converter = AppColors.categoryIndigo;
  static const Color apgar = AppColors.categoryMaroon;
  static const Color appleFull = AppColors.categoryPink;
  static const Color consent = AppColors.categoryOrange;
  static const Color checklist = AppColors.categoryBlue;
}
